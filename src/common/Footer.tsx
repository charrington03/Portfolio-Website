import React from "react";
import { Flex } from "theme-ui";

const Footer = (props: any) => {
	return (
		<Flex
			sx={{
				bg: "dark",
				color: "light",
				fontFamily: "heading",
				m: 0,
				p: 64,
			}}
		>
			<p>Footer kekw</p>
		</Flex>
	);
};

export default Footer;
